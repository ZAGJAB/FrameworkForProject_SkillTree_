<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="tsing.zhong.fu.frameworkforproject_skilltree_" >

    <!-- To access Google+ APIs: -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.USE_CREDENTIALS" /> <!-- To retrieve the account name (email) as part of sign-in: -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" /> <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="未命名"
        android:theme="@style/MDTheme" >
        <activity
            android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.ListActivity"
            android:label="List" >
        </activity>
        <activity
            android:name=".ui.AboutActivity"
            android:label="@string/title_activity_about" >
        </activity>
        <activity
            android:theme="@style/MDThemeActionBar"
            android:name=".ui.LoginActivity"
            android:label="注册"
            android:windowSoftInputMode="adjustResize|stateVisible" >
        </activity>
        <activity
            android:theme="@style/MDThemeActionBar"
            android:name=".ui.SettingsActivity"
            android:label="@string/title_activity_settings" >
        </activity>
        <activity android:name=".ui.Friends"
            android:label="朋友圈">

        </activity>
        <activity android:name=".ui.Hot">

        </activity>
        <activity android:name=".ui.About">

        </activity>
        <activity android:name=".ui.Message"
            android:label="消息">

        </activity>
        <activity android:name=".ui.Add"
            android:theme="@style/MDThemeActionBar"
            android:label="添加新课程">

        </activity>
        <activity android:name=".ui.FlexibleSpaceWithImageWithViewPagerTabActivity">

        </activity>
    </application>

</manifest>
